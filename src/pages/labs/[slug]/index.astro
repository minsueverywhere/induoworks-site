---
import LabDetailView from '../../../views/LabDetailView.astro';
import { labs } from '../../../config';

export function getStaticPaths() {
  return labs.map((lab) => ({ params: { slug: lab.slug }, props: { lab } }));
}

const { lab } = Astro.props;
---

<LabDetailView lang="en" lab={lab} />
